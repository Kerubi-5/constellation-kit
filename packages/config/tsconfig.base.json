{
  "$schema": "https://json.schemastore.org/tsconfig",
  "docs": "Shared TS base for constellation Next.js apps. Each repo extends this and adds its own `paths`, `include`, and any stricter flags (e.g. kairos's noUnusedLocals). Compiler options common to both live here.",
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [{ "name": "next" }]
  }
}
